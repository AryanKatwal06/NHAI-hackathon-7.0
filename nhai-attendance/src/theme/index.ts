// index.ts — Barrel export for the theme system.
export { ThemeProvider, useTheme } from './ThemeProvider';
export { DarkColors, LightColors, colors, type Colors } from './colors';
export { FontFamily, FontSize, FontWeight, LineHeight, typography } from './typography';
export { Spacing, BorderRadius, spacing, borderRadius } from './spacing';
export { theme } from './theme';
