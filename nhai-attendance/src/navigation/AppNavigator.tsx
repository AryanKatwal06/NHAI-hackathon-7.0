// AppNavigator.tsx
// Navigation stack for authenticated flows (enrollment, auth, dashboard).

import React from 'react';
import { View } from 'react-native';

const AppNavigator: React.FC = () => {
  return <View />;
};

export default AppNavigator;
