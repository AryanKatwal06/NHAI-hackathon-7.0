// AuthNavigator.tsx
// Navigation stack for unauthenticated flows (login, splash).

import React from 'react';
import { View } from 'react-native';

const AuthNavigator: React.FC = () => {
  return <View />;
};

export default AuthNavigator;
