# Repositories

Data access objects for each database table. Repositories encapsulate SQL queries and expose typed async methods. No raw SQL in screens or services.

Implemented in **Phase 2**.

**Rules:** One table per repository. All methods async with typed results. No business logic.
