# Liveness Detection

MediaPipe Face Mesh liveness pipeline: blink (EAR), smile (MAR), head turn (yaw), random challenge sequencer.

Implemented in **Phase 2**.

**Rules:** Random challenge selection. Strict timeouts. Frame Processor worklets.
